<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CartMapper">

	<insert id="cartAdd" parameterType="CartDTO">
	 insert into cart (Cart_Code,Member_Code,Cart_Amount,Item_Code,Cart_Size,Cart_Color,Cart_Taste)
	 values (cart_seq.nextval,#{Member_Code},#{Cart_Amount},#{Item_Code},#{Cart_Size},
	 #{Cart_Color},#{Cart_Taste})
	</insert>
	
	<select id="selectByCode" resultType="CartInfoDTO"  parameterType="int">
	
	select cart_code, member_code, cart_amount, cart_size, cart_color, cart_taste, c.item_code, 
		item_category, item_name, item_info, item_price, item_image, i.item_size, i.item_color, i.item_taste
		from cart c, item i where c.item_code = i.item_code and member_code = #{member_code}
	</select>
</mapper>